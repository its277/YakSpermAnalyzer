package com.yaksperm.analyzer.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Background Layers ─────────────────────────────────────────────────────────
val NavyBg     = Color(0xFF07111F)
val CardBg     = Color(0xFF0D1F33)
val CardAlt    = Color(0xFF112240)
val CardHover  = Color(0xFF142B47)

// ── Primary Accent — Cyan ─────────────────────────────────────────────────────
val CyanPrimary = Color(0xFF00C2D4)
val CyanLight   = Color(0xFF7EEAF5)
val CyanDim     = Color(0xFF1A4A5C)
val CyanGlow    = Color(0x3300C2D4)

// ── Semantic Colors ───────────────────────────────────────────────────────────
val GreenMotile  = Color(0xFF22D98A)
val GreenDim     = Color(0xFF0F3D2A)
val AmberCount   = Color(0xFFF5A623)
val AmberDim     = Color(0xFF3D2A0A)
val RedAlert     = Color(0xFFFF4757)
val RedDim       = Color(0xFF3D0A0A)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFD8E8F0)
val TextSecond  = Color(0xFF8FAFC0)
val TextDim     = Color(0xFF5A7A8A)

// ── Borders ───────────────────────────────────────────────────────────────────
val Border      = Color(0xFF162B3D)
val BorderLight = Color(0xFF1E3850)

// ── Gradients (start/end pairs used in custom drawing) ───────────────────────
val GradientCyanStart  = Color(0xFF00C2D4)
val GradientCyanEnd    = Color(0xFF006E7A)
val GradientGreenStart = Color(0xFF22D98A)
val GradientGreenEnd   = Color(0xFF0F7A4A)
